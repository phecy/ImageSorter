#include "common.h"

// Compute x^(1/3)
double root3(double num) {
    return pow(num, 1.0/3.0);
}

// Compute x^3
double pow3(double num) {
    return num*num*num;
}

// Compute x^2
double pow2(double num) {
    return num*num;
}
